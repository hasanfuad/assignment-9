import React from 'react';
import Header from '../../Header/Header';

const Blog = () => {
    return (
        <div>
            <Header/>
            <h1 style={{textAlign: 'center'}}>Blog is constructing..</h1>
        </div>
    );
};

export default Blog;